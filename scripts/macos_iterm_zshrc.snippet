# Optional zsh snippet for iTerm Shell Integration on macOS.
# This agent does not modify ~/.zshrc. Paste only if you want it.

# Faster prompt: skip heavy completions until after first prompt
# export BITBANK_BOT_ROOT="$HOME/docker-compose-up-d"
# if [ -x "$BITBANK_BOT_ROOT/.venv/bin/python" ]; then
#   alias bitbank-bot='PYTHONPATH="$BITBANK_BOT_ROOT/src" "$BITBANK_BOT_ROOT/.venv/bin/python" "$BITBANK_BOT_ROOT/main.py"'
# fi
# PYTHONUNBUFFERED=1 is already set by start.sh
